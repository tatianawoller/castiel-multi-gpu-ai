# Tools

This directory contains some information that might be useful to set up the
tool stack required for this training session.


## What is it?

1. `pipx_install.md`: instructions to install `dvc` using `pipx` (includes
   instructions to install `pipx`).

```{include} pipx_install.md
```